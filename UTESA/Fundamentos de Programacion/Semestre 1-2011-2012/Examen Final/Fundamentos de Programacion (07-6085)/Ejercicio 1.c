int main(){
    float numero = 3.15;
    float result;
    printf("Introduzca un numero en el rango de 1 a 500, para saber si es divisible entre 3:");
    scanf("%f", &numero);
    int result = numero % 3;
          if(result = 0){
         printf("El valor introducido es multiplo de 3", result);
         else {
              printf("El valor introducido no es multiplo de 3");
              printf("El valor introducido no esta dentro del rango", numero = 0, numero > 500);

}
    system("pause");    
}    
