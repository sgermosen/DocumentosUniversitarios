int main() {
    int num;
    scanf("%d",&num);
    if (num <= 500) {
            num % 3 = 0;
            printf("El numero es divisible entre 3\n");
    }else {
            printf("El numero no es divisible\n")
    }
system("PAUSE");   
}
